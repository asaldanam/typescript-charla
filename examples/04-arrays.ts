// # Arrays

// Para los arrays podemos emplear también dos sintaxis diferentes:

export const list1: Array<number> = [1, 2, 3];

export const list2: number[] = [1, 2, 3];

// Podemos crear nuestros propios tipos al igual que con los objetos

export type Numbers = number[];

export const list3: Numbers = [1, 2, 3];